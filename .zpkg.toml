[package]
org = "ores-wasm-loaders"
name = "owl-monorepo"
version = "0.1.0"
kind = "monorepo"
language = "workspace"
description = "ORES Wasm Loaders: the org's integration workspace"
license = "MIT"

[package.repository]
vcs = "git"
url = "https://github.com/ores-wasm-loaders/owl-monorepo"

[dependencies]


[build]
command = "node --test test/*.test.mjs"

[publish]
include_readme = true
tag_format = "v{version}"
exclude = [".env", ".env.*", ".vendor/**", ".zed/**", ".zed-pack/**", "tmp/**", "**/*.log", ".DS_Store", "**/target/**", "**/node_modules/**", "**/dist/**"]

[install]
dir = ".vendor/.zed"

[targets.repository]
dir = "."



[scripts]
test = "node --test test/*.test.mjs"
