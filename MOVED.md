# Moved

This repository is superseded by [`ores-wasm-loaders/owls-docs`](https://github.com/ores-wasm-loaders/owls-docs).

Two sessions built the fleet's shared WASM loading layer in parallel. The `owls-*`
packages are the ones that ship; everything here was merged into them, so this repo is
kept read-only for its history and its pull requests rather than deleted.

Nothing here is maintained. Read [`ores-wasm-loaders/owls-docs`](https://github.com/ores-wasm-loaders/owls-docs) instead.
